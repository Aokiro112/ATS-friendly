import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-sans",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "ResumeFlow — Build Your ATS-Friendly Resume in Minutes",
  description:
    "Create a professional, ATS-optimized resume in under 5 minutes. No account needed. Free forever.",
  keywords: [
    "resume builder",
    "ATS resume",
    "free resume maker",
    "professional resume",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col font-sans">{children}</body>
    </html>
  );
}
